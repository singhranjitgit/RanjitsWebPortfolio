export interface formItem {
    title: string;   
    controlName: string;  
    placeholderName: string;
  }

export interface placeholder{
    name: string;
    email: string;
    message: string;
}